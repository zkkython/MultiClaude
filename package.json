{
  "name": "multiclaude",
  "version": "1.0.7",
  "description": "Run multiple Claude Code configurations in parallel terminals",
  "main": "dist/main/index.js",
  "scripts": {
    "dev": "node scripts/dev.js",
    "build": "node scripts/build.js",
    "smoke": "node scripts/smoke.js",
    "eval:protocol": "node scripts/eval-protocol.js",
    "start": "electron dist/main/index.js",
    "postinstall": "electron-rebuild",
    "dist": "npm run build && electron-builder",
    "dist:mac": "npm run build && node scripts/dist-mac-keychain.js",
    "dist:win": "npm run build && electron-builder --win",
    "dist:mac:p12": "npm run build && electron-builder --mac",
    "dist:mac:nonotarize": "npm run build && node scripts/dist-mac-nonotarize.js",
    "test": "node scripts/test.js"
  },
  "author": "kason",
  "license": "MIT",
  "devDependencies": {
    "@electron/notarize": "^2.5.0",
    "@electron/rebuild": "^3.7.1",
    "electron": "^34.0.0",
    "electron-builder": "^25.1.8",
    "esbuild": "^0.24.2",
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "@xterm/addon-fit": "^0.10.0",
    "@xterm/addon-web-links": "^0.11.0",
    "@xterm/addon-webgl": "^0.18.0",
    "@xterm/xterm": "^5.5.0",
    "nanoid": "^5.0.9",
    "node-pty": "^1.1.0-beta22"
  }
}
